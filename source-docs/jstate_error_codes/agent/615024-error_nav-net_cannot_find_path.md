<!-- AUTO-GENERATED by generate_agent_docs.py; do not edit directly. -->
<!-- Source: nav-navigation-errors/615024-error_nav-net_cannot_find_path.md -->

# JState 错误码 615024：搜路结果为空

> Agent 视图：仅保留结论、诊断决策、证据/输出契约和 Playbook。完整原文见 [615024-error_nav-net_cannot_find_path.md](../nav-navigation-errors/615024-error_nav-net_cannot_find_path.md)。

## 1 错误结论
| 字段 | 内容 |
|---|---|
| 错误码 | `615024` |
| JState Key | `/jstate/error/nav-net/cannot_find_path` |
| 错误描述 | 搜路结果为空 |
| 等级 | `level5`（ERROR） |
| 直接触发条件 | 二维码检码任务中 `check.data.size() == 0`——从当前点到目标点的路径规划返回空结果 |
| 触发源码 | `qrcode_servo.cc:593` |

**核心判断**：在二维码检码流程中，需要从当前位置规划到待检二维码的路径。如果 `check.data` 为空（搜路失败），直接报 ERROR。注意 `check` 来自二维码检码任务数据。

| P1 | 路径不可达 | 路径规划 | 检查起点到目标的可达性 | 修复地图连通性或路径规划 |
| P1 | 起点/目标无效 | check.data 输入 | 确认起点和目标点位 | 修复检码任务数据 |

代码证据：JState key `health_info.hpp:99`，触发 `qrcode_servo.cc:593`

## 3 结合日志选择排查方向

最小证据范围为错误发生前后 60 秒的 `nav-net` 日志。只读检索：

```bash
grep -E '搜路结果为空' /opt/jz/log/nav-net.log | tail -n 200
```

| 顺序 | 检查项 | 正常判据 | 异常判据 | 结论状态与下一步 |
|---:|---|---|---|---|
| 1 | 原始错误上报 | 同一时间窗命中 `615024` 或直接触发日志 | 无匹配 | 扩大时间窗并核对进程、版本；标记 `证据不足` |
| 2 | 直接触发字段 | 日志字段与第 1 章源码条件一致 | 字段缺失或条件不一致 | 不推断现场根因，保留同期原始日志 |
| 3 | 上游关联日志 | 能找到触发前最早异常 | 只有结果码 | 输出 `待确认`，交由人工补充上游日志 |

停止规则：没有同期直接触发日志时，Agent 只能说明代码触发条件，不得把可能原因写成已确认根因。

## 5 修复验证与证据索引

Agent 最少输出错误时间窗、模块版本、原始错误行、直接触发字段、关联日志和缺失证据。验证仅检查同场景复测中错误是否再次出现，不执行配置修改、服务重启或车辆控制。

```yaml
error_code: "615024"
error_name: "cannot_find_path"
analysis_status: 待确认
applicable_version:
  primary_module: "nav-net"
confirmed_facts: []
most_likely_cause:
  cause: ""
  confidence: low
  evidence: []
missing_evidence: []
next_action: []
verification:
  result: not_started
  evidence: []
```

## 6 Agent 自动排查 Playbook

```yaml
playbook:
  meta:
    error_code: '615024'
    error_name: cannot_find_path
    module: nav-net
    time_window: 60
    read_only: true
  steps:
  - id: classify_trigger
    description: 确认直接触发日志
    checks:
    - type: grep_log
      module: nav-net
      pattern: 搜路结果为空
      on_match:
        conclusion: 已找到直接触发证据
        priority: P1
        goto: diagnose_upstream
      on_no_match:
        conclusion: 当前日志不足以确认触发链
        goto: insufficient_data
  - id: diagnose_upstream
    description: 检索上游字段与关联日志
    checks:
    - type: grep_log
      module: nav-net
      pattern: 搜路结果为空
      on_match:
        conclusion: 存在可供人工复核的上游证据
        priority: P1
        goto: verify
      on_no_match:
        conclusion: 只有结果码，不能确认现场根因
        goto: insufficient_data
  - id: insufficient_data
    description: 证据不足时停止归因
    checks:
    - type: grep_log
      module: nav-net
      pattern: 615024|cannot_find_path
      on_match:
        conclusion: 保留错误证据，根因待确认
        priority: P1
        goto: verify
      on_no_match:
        conclusion: insufficient_data
        goto: verify
  - id: verify
    description: 只读观察同场景复测
    checks:
    - type: verify_fix
      grep_pattern: 搜路结果为空
      watch_duration_sec: 60
      on_match:
        status: still_present
      on_no_match:
        status: not_observed
```
