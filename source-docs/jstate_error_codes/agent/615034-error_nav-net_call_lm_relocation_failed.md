<!-- AUTO-GENERATED by generate_agent_docs.py; do not edit directly. -->
<!-- Source: nav-navigation-errors/615034-error_nav-net_call_lm_relocation_failed.md -->

# JState 错误码 615034：调用反射板接口失败

> 当 `onRelocate()` 调用 `laser_mark_service->relocalization()` 抛出异常时触发，表示调用反射板重定位服务本身失败（非重定位结果失败）。

> Agent 视图：仅保留结论、诊断决策、证据/输出契约和 Playbook。完整原文见 [615034-error_nav-net_call_lm_relocation_failed.md](../nav-navigation-errors/615034-error_nav-net_call_lm_relocation_failed.md)。

## 1 错误结论

| 字段 | 内容 |
|---|---|
| 错误码 | `615034` |
| JState Key | `/jstate/error/nav-net/call_lm_relocation_failed` |
| 错误描述 | 调用反射板接口失败 |
| 等级 | `level1`（WARN） |
| 直接触发条件 | `onRelocate()` 中 `laser_mark_service->relocalization(result_pose, message)` 抛出 `std::exception` |
| 直接影响 | 反射板重定位中断，HTTP 返回 500 |
| META 来源 | `机型3代状态数据-META大全.xlsx` → `异常状态s3` |

**核心判断**：615034（调用失败/异常中断）与 615035（重定位结果失败）的区别：615034 是 `relocalization()` 本身抛异常（服务不可达或内部崩溃），615035 是正常返回但结果为失败。两者在 `onRelocate()` 中是互斥分支。

## 3 排查

```bash
grep -E "call_lm_relocation|LaserMarkRelocalization|反光板" /opt/jz/log/nav-net.log | tail -n 200
```

| 顺序 | 检查项 | 正常判据 | 异常判据 | 下一步 |
|---:|---|---|---|---|
| 1 | 异常信息 | e.what() 包含具体错误 | 无异常信息 | 检查 `relocalization` 服务状态 |
| 2 | 服务可用性 | LaserMarkRelocalization 服务可达 | 服务不可达或超时 | 修复 ROS service |

## 4 原因与方案

| 优先级 | 原因 | 负责链路 | 临时处置 | 根因修复 |
|---|---|---|---|---|
| P1 | ROS service 不可达 | LaserMarkRelocalizationClient | 重启服务 | 修复服务启动和通信 |
| P1 | 服务内部崩溃 | 反射板算法 | 重试 | 修复算法异常处理 |

代码证据：JState key `health_info.hpp:79`，触发 `position_recover.cc:269`

## 5 Playbook

## 5 修复验证与证据索引

Agent 最少输出错误时间窗、模块版本、原始错误行、直接触发字段、关联日志和缺失证据。验证仅检查同场景复测中错误是否再次出现，不执行配置修改、服务重启或车辆控制。

```yaml
error_code: "615034"
error_name: "call_lm_relocation_failed"
analysis_status: 待确认
applicable_version:
  primary_module: "nav-net"
confirmed_facts: []
most_likely_cause:
  cause: ""
  confidence: low
  evidence: []
missing_evidence: []
next_action: []
verification:
  result: not_started
  evidence: []
```

## 6 Agent 自动排查 Playbook

```yaml
playbook:
  meta:
    error_code: '615034'
    error_name: call_lm_relocation_failed
    module: nav-net
    time_window: 60
    read_only: true
  steps:
  - id: classify_trigger
    description: 确认直接触发日志
    checks:
    - type: grep_log
      module: nav-net
      pattern: LaserMarkRelocalization|call_lm_relocation
      on_match:
        conclusion: 已找到直接触发证据
        priority: P1
        goto: diagnose_upstream
      on_no_match:
        conclusion: 当前日志不足以确认触发链
        goto: insufficient_data
  - id: diagnose_upstream
    description: 检索上游字段与关联日志
    checks:
    - type: grep_log
      module: nav-net
      pattern: LaserMarkRelocalization
      on_match:
        conclusion: 存在可供人工复核的上游证据
        priority: P1
        goto: verify
      on_no_match:
        conclusion: 只有结果码，不能确认现场根因
        goto: insufficient_data
  - id: insufficient_data
    description: 证据不足时停止归因
    checks:
    - type: grep_log
      module: nav-net
      pattern: 615034|call_lm_relocation_failed
      on_match:
        conclusion: 保留错误证据，根因待确认
        priority: P1
        goto: verify
      on_no_match:
        conclusion: insufficient_data
        goto: verify
  - id: verify
    description: 只读观察同场景复测
    checks:
    - type: verify_fix
      grep_pattern: LaserMarkRelocalization|call_lm_relocation
      watch_duration_sec: 60
      on_match:
        status: still_present
      on_no_match:
        status: not_observed
```
