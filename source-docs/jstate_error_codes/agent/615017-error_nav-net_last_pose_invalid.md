<!-- AUTO-GENERATED by generate_agent_docs.py; do not edit directly. -->
<!-- Source: nav-navigation-errors/615017-error_nav-net_last_pose_invalid.md -->

# JState 错误码 615017：上一次保存的pose无效

> 当 `usingLastPose()` 从 `RSI->location_state_` 获取上一次保存位置后，调用 `locate(pose, "Dragging")` 验证失败时触发。

> Agent 视图：仅保留结论、诊断决策、证据/输出契约和 Playbook。完整原文见 [615017-error_nav-net_last_pose_invalid.md](../nav-navigation-errors/615017-error_nav-net_last_pose_invalid.md)。

## 1 错误结论

| 字段 | 内容 |
|---|---|
| 错误码 | `615017` |
| JState Key | `/jstate/error/nav-net/last_pose_invalid` |
| 错误描述 | 上一次保存的pose无效 |
| 等级 | `level1`（WARN） |
| 直接触发条件 | `usingLastPose()` 中 `locate(locate_pose, "Dragging")` 返回 false（定位验证失败或超时） |
| 直接影响 | `usingLastPose()` 返回 false，整体恢复定位失败 |
| META 来源 | `机型3代状态数据-META大全.xlsx` → `异常状态s3` |

**核心判断**：`usingLastPose()` 是 `handleNavStarted` 的兜底——当调度未提供有效 PoseId（`pose_ID <= 0`）时使用。它从 `RSI->location_state_` 取上一次保存的位姿再走 `locate()` 验证。如果上次保存的位姿已过期或当前定位系统无法匹配，触发 615017。

## 3 排查

```bash
grep -E "usingLastPose|5989|last_pose_invalid" /opt/jz/log/nav-net.log | tail -n 200
```

| 顺序 | 检查项 | 正常判据 | 异常判据 | 下一步 |
|---:|---|---|---|---|
| 1 | 触发日志 | `5989|usingLastPose, invalid worldPose` | 无 | 扩大时间窗 |
| 2 | location_state_ | 存在有效历史位姿 | 位姿数据为空或过期 | 需重新建立定位 |

## 4 原因与方案

| 优先级 | 原因与唯一特征 | 负责链路 | 临时处置 | 根因修复 |
|---|---|---|---|---|
| P1 | 上一次位姿过期：机器人已移动但未更新 `location_state_` | RSI 状态管理 | 使用有效 PoseId 手动恢复 | 确保定位更新时写入 location_state_ |
| P1 | 定位验证失败：位姿存在但 `checkRet()` 无法匹配 | 定位系统 | 重试或选择合适的恢复点 | 修复定位匹配算法 |

代码证据：JState key `health_info.hpp:61`，触发 `position_recover.cc:477`

## 5 Playbook

## 5 修复验证与证据索引

Agent 最少输出错误时间窗、模块版本、原始错误行、直接触发字段、关联日志和缺失证据。验证仅检查同场景复测中错误是否再次出现，不执行配置修改、服务重启或车辆控制。

```yaml
error_code: "615017"
error_name: "last_pose_invalid"
analysis_status: 待确认
applicable_version:
  primary_module: "nav-net"
confirmed_facts: []
most_likely_cause:
  cause: ""
  confidence: low
  evidence: []
missing_evidence: []
next_action: []
verification:
  result: not_started
  evidence: []
```

## 6 Agent 自动排查 Playbook

```yaml
playbook:
  meta:
    error_code: '615017'
    error_name: last_pose_invalid
    module: nav-net
    time_window: 60
    read_only: true
  steps:
  - id: classify_trigger
    description: 确认直接触发日志
    checks:
    - type: grep_log
      module: nav-net
      pattern: 5989|usingLastPose, invalid
      on_match:
        conclusion: 已找到直接触发证据
        priority: P1
        goto: diagnose_upstream
      on_no_match:
        conclusion: 当前日志不足以确认触发链
        goto: insufficient_data
  - id: diagnose_upstream
    description: 检索上游字段与关联日志
    checks:
    - type: grep_log
      module: nav-net
      pattern: 5989|usingLastPose
      on_match:
        conclusion: 存在可供人工复核的上游证据
        priority: P1
        goto: verify
      on_no_match:
        conclusion: 只有结果码，不能确认现场根因
        goto: insufficient_data
  - id: insufficient_data
    description: 证据不足时停止归因
    checks:
    - type: grep_log
      module: nav-net
      pattern: 615017|last_pose_invalid
      on_match:
        conclusion: 保留错误证据，根因待确认
        priority: P1
        goto: verify
      on_no_match:
        conclusion: insufficient_data
        goto: verify
  - id: verify
    description: 只读观察同场景复测
    checks:
    - type: verify_fix
      grep_pattern: 5989|usingLastPose, invalid
      watch_duration_sec: 60
      on_match:
        status: still_present
      on_no_match:
        status: not_observed
```
