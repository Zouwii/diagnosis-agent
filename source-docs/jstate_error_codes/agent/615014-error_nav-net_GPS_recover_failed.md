<!-- AUTO-GENERATED by generate_agent_docs.py; do not edit directly. -->
<!-- Source: nav-navigation-errors/615014-error_nav-net_GPS_recover_failed.md -->

# JState 错误码 615014：使用GPS恢复定位失败

> 文档用途：当 AI Agent 在现场日志中发现 `615014` 时，用本文理解 GPS 定位恢复流程，判断失败在 checkGPSLocatingRet 超时还是异常中断。

> Agent 视图：仅保留结论、诊断决策、证据/输出契约和 Playbook。完整原文见 [615014-error_nav-net_GPS_recover_failed.md](../nav-navigation-errors/615014-error_nav-net_GPS_recover_failed.md)。

## 1 错误结论

| 字段 | 内容 |
|---|---|
| 错误码 | `615014` |
| JState Key | `/jstate/error/nav-net/GPS_recover_failed` |
| 错误描述 | 使用GPS恢复定位失败 |
| 等级 | `level1`（WARN） |
| 直接触发条件 | `usingGPS()` 中 `checkGPSLocatingRet()` 抛出异常（通常为定位系统不可达） |
| 直接影响 | GPS 恢复失败，`usingGPS` 返回 false；若 lasermark 也失败则整体恢复失败 |
| 本文验证基线 | `RC/1.2603.x @ 744cfcbe352b` |
| 适用前提 | `robot_info_["laserType"] == "3d"`（仅 3D 激光机型走 GPS 恢复） |
| META 来源 | `机型3代状态数据-META大全.xlsx` → `异常状态s3` |

**核心判断**：`615014`（GPS 恢复失败，异常中断）与 `615016`（GPS 定位超时）的区别：615014 是 `checkGPSLocatingRet()` 本身抛出异常导致 `try-catch` 捕获，615016 是轮询循环中时间超过 `kCheckTimeout=10s` 而正常退出。

## 3 结合日志选择排查方向

```bash
grep -E "GPS recover|GPS.*failed|usingGPS|checkGPSLocatingRet|GPS locate timeout"   /opt/jz/log/nav-net.log | tail -n 200
```

| 顺序 | 检查项 | 正常判据 | 异常判据 | 结论状态与下一步 |
|---:|---|---|---|---|
| 1 | 触发分支 | `GPS recover position failed` + 异常信息 | 仅有 `GPS locate timeout`（615016） | 异常 vs 超时→区分根因 |
| 2 | RTK 定位状态 | `/vtr/localization` 中 localization_type == "rtk_MANUAL" | 定位类型不匹配 | RTK 模式未启用→`已确认` |
| 3 | laserType 配置 | `robot_info_["laserType"]=="3d"` | 非 3d 但走了 GPS 恢复 | 配置错误→`已确认` |

## 5 代码证据

| 证据 | 位置 |
|---|---|
| JState key | `health_info.hpp:117` → `kGPSRecoverFailed` |
| 触发调用 | `position_recover.cc:356` |
| checkGPSLocatingRet | `position_recover.cc:442-467` |

## 6 Agent 自动排查 Playbook

## 5 修复验证与证据索引

Agent 最少输出错误时间窗、模块版本、原始错误行、直接触发字段、关联日志和缺失证据。验证仅检查同场景复测中错误是否再次出现，不执行配置修改、服务重启或车辆控制。

```yaml
error_code: "615014"
error_name: "GPS_recover_failed"
analysis_status: 待确认
applicable_version:
  primary_module: "nav-net"
confirmed_facts: []
most_likely_cause:
  cause: ""
  confidence: low
  evidence: []
missing_evidence: []
next_action: []
verification:
  result: not_started
  evidence: []
```

## 6 Agent 自动排查 Playbook

```yaml
playbook:
  meta:
    error_code: '615014'
    error_name: GPS_recover_failed
    module: nav-net
    time_window: 60
    read_only: true
  steps:
  - id: classify_trigger
    description: 确认直接触发日志
    checks:
    - type: grep_log
      module: nav-net
      pattern: GPS recover position failed
      on_match:
        conclusion: 已找到直接触发证据
        priority: P1
        goto: diagnose_upstream
      on_no_match:
        conclusion: 当前日志不足以确认触发链
        goto: insufficient_data
  - id: diagnose_upstream
    description: 检索上游字段与关联日志
    checks:
    - type: grep_log
      module: nav-net
      pattern: 5987|usingGPS failed
      on_match:
        conclusion: 存在可供人工复核的上游证据
        priority: P1
        goto: verify
      on_no_match:
        conclusion: 只有结果码，不能确认现场根因
        goto: insufficient_data
  - id: insufficient_data
    description: 证据不足时停止归因
    checks:
    - type: grep_log
      module: nav-net
      pattern: 615014|GPS_recover_failed
      on_match:
        conclusion: 保留错误证据，根因待确认
        priority: P1
        goto: verify
      on_no_match:
        conclusion: insufficient_data
        goto: verify
  - id: verify
    description: 只读观察同场景复测
    checks:
    - type: verify_fix
      grep_pattern: GPS recover position failed
      watch_duration_sec: 60
      on_match:
        status: still_present
      on_no_match:
        status: not_observed
```
