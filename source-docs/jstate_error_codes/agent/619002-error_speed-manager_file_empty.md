<!-- AUTO-GENERATED by generate_agent_docs.py; do not edit directly. -->
<!-- Source: nav-navigation-errors/619002-error_speed-manager_file_empty.md -->

# JState 错误码 619002：odom文件为空，请检查map/odom_info.json是否有数据

> 文档用途：当 AI Agent 在现场日志中发现 `619002` 时，用本文理解错误含义和所属模块，并根据同期日志选择排查方向。

> Agent 视图：仅保留结论、诊断决策、证据/输出契约和 Playbook。完整原文见 [619002-error_speed-manager_file_empty.md](../nav-navigation-errors/619002-error_speed-manager_file_empty.md)。

## 1 错误结论

| 字段 | 内容 |
|---|---|
| 错误码 | `619002` |
| JState Key | `/jstate/error/speed-manager/file_empty` |
| 错误描述 | odom文件为空，请检查map/odom_info.json是否有数据 |
| 等级 | `level5`，nav-net（导航网络模块） 内部为 `ERROR` |
| 所属模块 | `file_empty` |
| 直接触发条件 | **当前工作区缺少 file_empty 源码**。META 描述：odom文件为空，请检查map/odom_info.json是否有数据。 |
| 直接影响 | 根据具体触发场景，可能影响当前导航任务或相关功能。 |
| 自动恢复条件 | 错误级别：触发态最多发布 3 次后静默，依赖任务重置清除。 |
| 本文验证基线 | `RC/1.2603.x @ 744cfcbe352b` |
| 适用前提 | 机器人运行版本包含该错误码注册和 file_empty 模块 |
| META 来源 | `机型3代状态数据-META大全.xlsx` → `异常状态s3` |
| META 描述（英文） | the odom file is empty, please check if there is any data in map/odom_info.json |
| META 外部说明 | [odom文件为空，请检查map/odom_info.json是否有数据](https://alidocs.dingtalk.com/i/nodes/Qnp9zOoBVBZ7nY4DHrmnY1XPV1DK0g6l?utm_scene=team_space) |

**核心判断**：`619002` 属于 `file_empty` 模块的错误码。file_empty 是独立仓库，源码不在当前 nav-manager 工作区中。基于 META 注册信息，该错误表示 **odom文件为空，请检查map/odom_info.json是否有数据**。缺少源码时，只能根据 META 描述和模块功能推断触发分支，不能确定具体代码路径。

本文使用以下证据状态：

| 状态 | 含义 |
|---|---|
| `已确认` | META 注册信息可直接证明 |
| `高可能` | 特征与某一原因高度吻合，但仍缺少源码侧直接证据 |
| `待确认` | 当前只能作为排查方向 |
| `已排除` | 正常判据或反向证据已经排除该原因 |

## 3 结合日志选择排查方向

先提取错误前后至少 60 秒的 file_empty 日志：

```bash
grep -E "error_speed-manager_file_empty|file_empty|error" /opt/jz/log/nav-net.log | tail -n 300
```

按顺序执行：

| 顺序 | 检查项 | 正常判据 | 异常判据 | 结论状态与下一步 |
|---:|---|---|---|---|
| 1 | 时间与版本 | JState、日志和任务属于同一时间窗 | 时间、版本或日志不一致 | 标记`待确认`并重新取证 |
| 2 | 错误确认 | 能从 `/route/error` 确认 `error_speed-manager_file_empty` 出现 | JState 中出现但日志中无对应事件 | 扩大时间窗或检查 file_empty 日志级别 |
| 3 | 同模块关联错误 | 无其他 file_empty 错误同时出现 | 多个 file_empty 错误码同时出现 | 优先分析最早出现的错误或模块级异常 |
| 4 | 上游依赖 | 依赖的服务、topic 和数据源均正常 | topic 无发布者、服务不可达、数据异常 | 按数据中断链追因 |
| 5 | 当前任务上下文 | 任务正常执行中 | 任务在特定阶段反复触发 | 关联任务阶段和模块内部状态 |
| 6 | 复测 | 同一场景连续执行不再触发 | 复测仍触发 | 需要 file_empty 源码排查具体失败分支 |

停止规则：

- 缺少 file_empty 源码时，不能输出源码级根因。`
- 只有错误码没有同期日志时，只能解释 META 含义。`
- 修复后同场景连续多次不再触发方可闭环。`

现场确认命令：

```bash
# 确认 JState 原始上报
rostopic echo -n 1 /route/error

# 检查 file_empty 相关节点
rosnode list | grep -E "file_empty|nav"

# 查看错误前后的完整日志上下文
grep -B 30 -A 30 "error_speed-manager_file_empty" /opt/jz/log/nav-net.log
```

## 5 修复验证与证据索引

闭环需要同时满足：

1. 已确认错误来源为 file_empty 模块。
2. `//jstate/error/speed-manager/file_empty` 不再上报 `trigger:true`。`
3. 同场景连续执行多次不再触发 `619002`。
4. 关联的上游数据和服务正常。

Agent 最少需要收集：

```text
错误时间及前后至少 60 秒的未过滤日志
/route/error 原始消息
file_empty 同时间窗日志
相关 ROS topic 和服务状态
file_empty 和 nav-manager 版本
修复后同场景复测结果
```

Agent 输出格式：

```yaml
error_code: 619002
error_name: file_empty
analysis_status: 待确认  # 已确认 / 高可能 / 待确认 / 已排除
applicable_version:
  primary_module: "file_empty"
  nav_manager: ""
trigger_path: unknown
trigger_stage: unknown
confirmed_facts:
  - "META 注册为 file_empty 模块错误"
most_likely_cause:
  cause: ""
  confidence: low  # high / medium / low
  evidence:
    - ""
excluded_causes: []
missing_evidence:
  - "file_empty 源码不在当前仓库，无法确认具体触发分支"
next_action:
  - ""
temporary_action: ""
root_fix: ""
verification:
  result: not_started  # passed / failed / not_started
  evidence:
    - ""
```

输出约束：

- 缺少 file_empty 源码时，`trigger_path` 保持 `unknown`。
- 根因必须是经源码证实的，不得用 META 描述代替根因。
- 未执行同场景复测时，验证结果必须为 `not_started`。

## 6 Agent 自动排查 Playbook

> 本章供 diagnosis-orchestrator 的确定性分析器自动执行。由于缺少 file_empty 源码，playbook 基于 META 注册信息和模块功能构建。

```yaml
playbook:
  meta:
    error_code: "619002"
    error_name: "file_empty"
    module: "file_empty"
    time_window: 60

  steps:
    - id: classify_trigger_chain
      description: "确认 error_speed-manager_file_empty 是否在时间窗内出现"
      checks:
        - type: grep_log
          module: "file_empty"
          pattern: "error_speed-manager_file_empty"
          on_match:
            trigger_chain: "file_empty_confirmed"
            conclusion: "命中 error_speed-manager_file_empty，确认为 file_empty 模块错误"
            goto: diagnose_file_empty
        - type: on_no_match
          conclusion: "当前时间窗内未找到 error_speed-manager_file_empty，扩大时间窗或检查 JState 原始消息"
          action: expand_time_window
          time_window: 120

    - id: diagnose_file_empty
      description: "排查 error_speed-manager_file_empty 的可能原因：关联错误 → 模块状态 → 上游依赖"
      evidence_boundary: true
      checks:
        - type: grep_log
          module: "file_empty"
          pattern: "error|fail|timeout|invalid|not found"
          priority: P1
          on_match:
            conclusion: "file_empty 模块存在异常日志，需逐条分析"
            continue: true
          on_no_match:
            conclusion: "无其他异常日志，error_speed-manager_file_empty 可能是独立触发"
            continue: true
        - type: grep_log
          module: "file_empty"
          pattern: "error_speed-manager_file_empty"
          priority: P1
          context_lines: 10
          on_match:
            conclusion: "找到 error_speed-manager_file_empty 日志上下文，检查触发前后的数据和状态"
            continue: true
          on_no_match:
            continue: true
        - type: on_no_match
          conclusion: "缺少 file_empty 源码，无法进一步确定触发分支"
          status: insufficient_data
          action: "获取 file_empty 源码后补充具体排查步骤"

    - id: verify
      description: "验证修复：连续 60 秒不再出现 error_speed-manager_file_empty"
      checks:
        - type: verify_fix
          grep_pattern: "error_speed-manager_file_empty"
          watch_duration_sec: 60
          on_match:
            status: still_present
            action: "错误仍然存在，需获取 file_empty 源码深入排查"
          on_no_match:
            status: verified
            conclusion: "连续 60 秒未出现 error_speed-manager_file_empty，错误已清除"
```
