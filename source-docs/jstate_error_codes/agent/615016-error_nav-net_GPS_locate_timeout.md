<!-- AUTO-GENERATED by generate_agent_docs.py; do not edit directly. -->
<!-- Source: nav-navigation-errors/615016-error_nav-net_GPS_locate_timeout.md -->

# JState 错误码 615016：使用GPS恢复定位超时

> 与 615015 同类，但用于 GPS/RTK 定位。`checkGPSLocatingRet()` 轮询 10s 等待 `localization_type == "rtk_MANUAL"` 且 `pose_valid == true`。

> Agent 视图：仅保留结论、诊断决策、证据/输出契约和 Playbook。完整原文见 [615016-error_nav-net_GPS_locate_timeout.md](../nav-navigation-errors/615016-error_nav-net_GPS_locate_timeout.md)。

## 1 错误结论

| 字段 | 内容 |
|---|---|
| 错误码 | `615016` |
| JState Key | `/jstate/error/nav-net/GPS_locate_timeout` |
| 错误描述 | 使用GPS恢复定位超时 |
| 等级 | `level1`（WARN） |
| 直接触发条件 | `checkGPSLocatingRet()` 中 `ros::Time::now() - start_time > kCheckTimeout(10s)`：每 0.2s 检查一次 `locate_ret_ > 0` 且 `localization_type == rtk_MANUAL` |
| 直接影响 | GPS 恢复失败，`usingGPS()` 返回 false |
| META 来源 | `机型3代状态数据-META大全.xlsx` → `异常状态s3` |

**核心判断**：与 615015 对称——615015 检查 `laser_MANUAL`，615016 检查 `rtk_MANUAL`。两者的退出条件和超时逻辑一致。

## 3 排查

```bash
grep -E "GPS locate timeout|5983|rtk_MANUAL|checkGPSLocatingRet" /opt/jz/log/nav-net.log | tail -n 200
```

| 顺序 | 检查项 | 正常判据 | 异常判据 | 下一步 |
|---:|---|---|---|---|
| 1 | 超时日志 | `5983|GPS locate timeout` | 无 | 非超时分支→检查 5980 |
| 2 | RTK 模式 | localization_type == rtk_MANUAL | 一直是其他类型 | RTK 模式未激活 |

## 4 原因与方案

| 优先级 | 原因与唯一特征 | 负责链路 | 临时处置 | 根因修复 |
|---|---|---|---|---|
| P1 | RTK 定位无响应：locate_ret_ 始终 0 | RTK 驱动、/vtr/localization | 重试或切换定位方式 | 修复 RTK 数据发布 |
| P1 | RTK 类型不匹配：类型非 rtk_MANUAL | 定位模式 | 确认 RTK 模式 | 修复模式切换 |

代码证据：JState key `health_info.hpp:113`，触发 `position_recover.cc:462`，kCheckTimeout=10s `position_recover.h:75`

## 5 Playbook

## 5 修复验证与证据索引

Agent 最少输出错误时间窗、模块版本、原始错误行、直接触发字段、关联日志和缺失证据。验证仅检查同场景复测中错误是否再次出现，不执行配置修改、服务重启或车辆控制。

```yaml
error_code: "615016"
error_name: "GPS_locate_timeout"
analysis_status: 待确认
applicable_version:
  primary_module: "nav-net"
confirmed_facts: []
most_likely_cause:
  cause: ""
  confidence: low
  evidence: []
missing_evidence: []
next_action: []
verification:
  result: not_started
  evidence: []
```

## 6 Agent 自动排查 Playbook

```yaml
playbook:
  meta:
    error_code: '615016'
    error_name: GPS_locate_timeout
    module: nav-net
    time_window: 60
    read_only: true
  steps:
  - id: classify_trigger
    description: 确认直接触发日志
    checks:
    - type: grep_log
      module: nav-net
      pattern: GPS locate timeout
      on_match:
        conclusion: 已找到直接触发证据
        priority: P1
        goto: diagnose_upstream
      on_no_match:
        conclusion: 当前日志不足以确认触发链
        goto: insufficient_data
  - id: diagnose_upstream
    description: 检索上游字段与关联日志
    checks:
    - type: grep_log
      module: nav-net
      pattern: rtk_MANUAL
      on_match:
        conclusion: 存在可供人工复核的上游证据
        priority: P1
        goto: verify
      on_no_match:
        conclusion: 只有结果码，不能确认现场根因
        goto: insufficient_data
  - id: insufficient_data
    description: 证据不足时停止归因
    checks:
    - type: grep_log
      module: nav-net
      pattern: 615016|GPS_locate_timeout
      on_match:
        conclusion: 保留错误证据，根因待确认
        priority: P1
        goto: verify
      on_no_match:
        conclusion: insufficient_data
        goto: verify
  - id: verify
    description: 只读观察同场景复测
    checks:
    - type: verify_fix
      grep_pattern: GPS locate timeout
      watch_duration_sec: 60
      on_match:
        status: still_present
      on_no_match:
        status: not_observed
```
