<!-- AUTO-GENERATED by generate_agent_docs.py; do not edit directly. -->
<!-- Source: nav-navigation-errors/615020-error_nav-net_current_pose_no_qrcode_data.md -->

# JState 错误码 615020：当前位置无二维码数据

> Agent 视图：仅保留结论、诊断决策、证据/输出契约和 Playbook。完整原文见 [615020-error_nav-net_current_pose_no_qrcode_data.md](../nav-navigation-errors/615020-error_nav-net_current_pose_no_qrcode_data.md)。

## 1 错误结论
| 字段 | 内容 |
|---|---|
| 错误码 | `615020` |
| JState Key | `/jstate/error/nav-net/current_pose_no_qrcode_data` |
| 错误描述 | 当前位置无二维码数据 |
| 等级 | `level5`（ERROR） |
| 直接触发条件 | `onToggleNav()` 中 `getStartPose()` 返回的 `start_pose.qrcode_ID < 0`——机器人当前位置无法关联到任何二维码矩阵区域 |
| 触发源码 | `qrcode_servo.cc:356` |

```mermaid
flowchart TD
    A[onToggleNav: 启动导航] --> B[getStartPose rest_path]
    B --> C{返回的 start_pose.qrcode_ID}
    C -- "< 0" --> D[LOGE 5991|current pose no qrcode data]
    D --> E[pubJ3State kCurrentPoseNoQrcodeData, level5]
    E --> F[/route/error 615020 ERROR]
    C -- ">= 0" --> G[正常获取二维码区域]
```

```text
onToggleNav() @ qrcode_servo.cc:349-359
  -> start_pose = getStartPose(rest_path)
  -> if start_pose.qrcode_ID < 0
  -> pubJ3State(kCurrentPoseNoQrcodeData, 5, "当前位置无二维码数据")
```

getStartPose() 优先从剩余路径中找有 qrcode_ID 的点（`qrcode_ID >= 0`），找不到则用 nearbyPose 搜 2m 内最近点。两个都失败时返回 qrcode_ID < 0 的 start_pose。

| P1 | 当前位置无二维码覆盖 | rest_path 路径点、地图 | 移动机器人到二维码区域 | 确保地图覆盖全部作业区域 |
| P1 | 定位错误：机器人实际位置与地图不符 | updateTf | 检查 map→base_footprint tf | 修复定位 |

代码证据：JState key `health_info.hpp:107`，触发 `qrcode_servo.cc:356`

## 3 结合日志选择排查方向

最小证据范围为错误发生前后 60 秒的 `nav-net` 日志。只读检索：

```bash
grep -E '5991|current pose no qrcode data' /opt/jz/log/nav-net.log | tail -n 200
```

| 顺序 | 检查项 | 正常判据 | 异常判据 | 结论状态与下一步 |
|---:|---|---|---|---|
| 1 | 原始错误上报 | 同一时间窗命中 `615020` 或直接触发日志 | 无匹配 | 扩大时间窗并核对进程、版本；标记 `证据不足` |
| 2 | 直接触发字段 | 日志字段与第 1 章源码条件一致 | 字段缺失或条件不一致 | 不推断现场根因，保留同期原始日志 |
| 3 | 上游关联日志 | 能找到触发前最早异常 | 只有结果码 | 输出 `待确认`，交由人工补充上游日志 |

停止规则：没有同期直接触发日志时，Agent 只能说明代码触发条件，不得把可能原因写成已确认根因。

## 5 修复验证与证据索引

Agent 最少输出错误时间窗、模块版本、原始错误行、直接触发字段、关联日志和缺失证据。验证仅检查同场景复测中错误是否再次出现，不执行配置修改、服务重启或车辆控制。

```yaml
error_code: "615020"
error_name: "current_pose_no_qrcode_data"
analysis_status: 待确认
applicable_version:
  primary_module: "nav-net"
confirmed_facts: []
most_likely_cause:
  cause: ""
  confidence: low
  evidence: []
missing_evidence: []
next_action: []
verification:
  result: not_started
  evidence: []
```

## 6 Agent 自动排查 Playbook

```yaml
playbook:
  meta:
    error_code: '615020'
    error_name: current_pose_no_qrcode_data
    module: nav-net
    time_window: 60
    read_only: true
  steps:
  - id: classify_trigger
    description: 确认直接触发日志
    checks:
    - type: grep_log
      module: nav-net
      pattern: 5991|current pose no qrcode data
      on_match:
        conclusion: 已找到直接触发证据
        priority: P1
        goto: diagnose_upstream
      on_no_match:
        conclusion: 当前日志不足以确认触发链
        goto: insufficient_data
  - id: diagnose_upstream
    description: 检索上游字段与关联日志
    checks:
    - type: grep_log
      module: nav-net
      pattern: getStartPose|can
      on_match:
        conclusion: 存在可供人工复核的上游证据
        priority: P1
        goto: verify
      on_no_match:
        conclusion: 只有结果码，不能确认现场根因
        goto: insufficient_data
  - id: insufficient_data
    description: 证据不足时停止归因
    checks:
    - type: grep_log
      module: nav-net
      pattern: 615020|current_pose_no_qrcode_data
      on_match:
        conclusion: 保留错误证据，根因待确认
        priority: P1
        goto: verify
      on_no_match:
        conclusion: insufficient_data
        goto: verify
  - id: verify
    description: 只读观察同场景复测
    checks:
    - type: verify_fix
      grep_pattern: 5991|current pose no qrcode data
      watch_duration_sec: 60
      on_match:
        status: still_present
      on_no_match:
        status: not_observed
```
