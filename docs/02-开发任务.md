# 开发任务清单

> 日期：2026-08-04
> 稳定任务编号，区分 Now（当前实现基线）与 Later（后续候选项）

---

## 任务编号约定

- `D` = Diagnosis 核心流程
- `S` = Service 服务与集成
- `K` = Knowledge 知识与证据
- `O` = Operations 可观测性与闭环
- `T` = Test & Quality 测试与质量

每个任务的状态为：`NOW`（当前实现基线，可能仍有环境验收或接线待办）或 `LATER`（后续候选项，不承诺排序）。

---

## Now：当前实现基线

### D-01：端到端单节点图编译通过

| 字段 | 内容 |
|---|---|
| **编号** | D-01 |
| **状态** | NOW |
| **描述** | LangGraph StateGraph 以单节点 `run_cli_workflow` 编译并通过基础拓扑测试 |
| **关键文件** | `agent/graph.py`、`agent/state.py`、`agent/nodes/run_cli.py` |
| **当前进度** | 图编译通过，`test_graph_topology.py` 已覆盖节点/边/状态结构与 checkpointer |
| **待办** | — |

### D-02：四个采集入口接入 engine collectors

| 字段 | 内容 |
|---|---|
| **编号** | D-02 |
| **状态** | NOW |
| **描述** | `agent/nodes/collect.py` 统一接入 `engine/collectors/dispatch.py`，支持 `internal_robot`、`tb_task`、`remote_site`、`local_logs` 四路采集 |
| **关键文件** | `agent/nodes/collect.py`、`engine/collectors/`（dispatch, ssh_robot, teambition, remote_hand, local_logs） |
| **当前进度** | 四路采集路径已实现，本地日志路径校验已生效，采集单元测试已覆盖主流场景 |
| **待办** | SSH/TB 采集在真实环境验收 |

### D-03：engine 全部模块迁移完成并可 import

| 字段 | 内容 |
|---|---|
| **编号** | D-03 |
| **状态** | NOW |
| **描述** | 从 `diagnosis-orchestrator` 迁移的全部 engine 模块（analysis、collectors、reporting、workflows）在项目内直接 import，不通过子进程 CLI |
| **关键文件** | `engine/` 下全部 30+ py 文件 |
| **当前进度** | 全部文件已迁移，import 路径修复完毕 |
| **待办** | 部分模块待 Case 数据验收 |

### D-04：模式注册表与固定白名单

| 字段 | 内容 |
|---|---|
| **编号** | D-04 |
| **状态** | NOW |
| **描述** | `engine/mode_registry.py` 实现四种固定 mode→Skill 映射，不接受客户端传任意 Skill 名称 |
| **关键文件** | `engine/mode_registry.py`、`tests/test_mode_registry.py` |
| **当前进度** | 四模式白名单已实现，参数校验已覆盖，单元测试通过 |

### S-01：FastAPI Case API + SSE 进度推送

| 字段 | 内容 |
|---|---|
| **编号** | S-01 |
| **状态** | NOW |
| **描述** | Case CRUD（创建/查询/流式进度/报告下载）+ 日志上传 |
| **关键文件** | `server/main.py`、`server/api/cases.py` |
| **当前进度** | 5 个 API 端点已实现，SSE 已接持久化 progress_events |
| **待办** | — |

### S-02：独立 Worker 文件队列

| 字段 | 内容 |
|---|---|
| **编号** | S-02 |
| **状态** | NOW |
| **描述** | 独立进程 Worker 通过文件队列轮询 pending Case，使用 `.worker.lock` 防重复 |
| **关键文件** | `server/worker.py`、`server/case_runner.py` |
| **当前进度** | Worker 主循环、锁机制、Case runner 已实现 |
| **待办** | Worker 生命周期管理（systemd 集成 / 优雅关闭确认） |

### S-03：双源身份认证

| 字段 | 内容 |
|---|---|
| **编号** | S-03 |
| **状态** | NOW |
| **描述** | management-system Session Cookie 验证 + 内部 Header Token 验证 |
| **关键文件** | `server/auth.py`、`server/identity.py` |
| **当前进度** | 双源认证已实现，生产环境禁止匿名访问 |
| **待办** | — |

### S-04：租户隔离存储

| 字段 | 内容 |
|---|---|
| **编号** | S-04 |
| **状态** | NOW |
| **描述** | owner_hash 隔离的持久化目录布局，Case 和上传的租户边界校验 |
| **关键文件** | `engine/tenant_storage.py`、`tests/test_tenant_storage.py` |
| **当前进度** | 完整实现已验证，单元测试覆盖边界条件 |
| **待办** | — |

### K-01：Playbook 加载器

| 字段 | 内容 |
|---|---|
| **编号** | K-01 |
| **状态** | NOW |
| **描述** | 从 `source-docs/jstate_error_codes/agent/` 加载 54 个 Playbook，执行确定性检查和触发链匹配 |
| **关键文件** | `knowledge/playbook_loader.py` |
| **当前进度** | 54/54 Playbook 可加载，解析正确，跳转目标无悬空 |
| **待办** | 在引擎分析流程中接入 Playbook 执行结果 |

### K-02：TopK 现场文档关键词匹配

| 字段 | 内容 |
|---|---|
| **编号** | K-02 |
| **状态** | NOW |
| **描述** | 按错误码和问题类型的 B 类文档关键词匹配 |
| **关键文件** | `knowledge/topk_loader.py` |
| **当前进度** | 50 个错误码映射、5 个问题类型、symptom 关键词匹配已实现 |
| **待办** | 在引擎分析流程中接入匹配结果 |

---

## Later：后续候选项

### D-10：接入 Playbook 与 TopK 匹配到引擎分析管道

| 字段 | 内容 |
|---|---|
| **编号** | D-10 |
| **状态** | LATER |
| **描述** | 在 `run_case` 工作流中将 Playbook 执行结果和 TopK 关键词匹配作为分析输入，补充确定性分析 |
| **依赖** | K-01、K-02 |
| **说明** | Playbook 加载器和 TopK 匹配器已实现，待接入 `engine/workflows/run_case.py` 的分析流水线 |

### D-11：错误码关联图

| 字段 | 内容 |
|---|---|
| **编号** | D-11 |
| **状态** | LATER |
| **描述** | 建立错误码因果图，支持拓扑排序确定排查优先级 |
| **依赖** | source-docs 错误码文档体系 |
| **说明** | 当前 `knowledge/` 尚无 correlation_graph.yaml |

### D-12：增加 analysis_agent 节点

| 字段 | 内容 |
|---|---|
| **编号** | D-12 |
| **状态** | LATER |
| **描述** | 在 `run_cli_workflow` 之后增加独立的 `analysis_agent` 节点，用 LLM 做深度分析 |
| **依赖** | D-01 |
| **说明** | 当前图只有单节点；`analysis_agent` 作为后续节点追加，不做多路 fan-out |

### D-13：增加 fusion_agent 节点

| 字段 | 内容 |
|---|---|
| **编号** | D-13 |
| **状态** | LATER |
| **描述** | 融合确定性分析和 LLM 深度分析的结论，输出最终诊断 |
| **依赖** | D-12 |
| **说明** | State 中已预留 `analysis_agent_result`、`fusion_result` 字段 |

### D-14：报告节点接入 engine reporting 模块

| 字段 | 内容 |
|---|---|
| **编号** | D-14 |
| **状态** | LATER |
| **描述** | engine/reporting/ 的报告渲染模块接入 Case 流程，生成完整格式化的诊断报告 |
| **依赖** | D-01 |
| **说明** | engine reporting 模块已迁移，当前 `run_cli_workflow` 内联生成了基础报告 |

### S-10：Checkpointer 升级 PostgresSaver 并通过验证

| 字段 | 内容 |
|---|---|
| **编号** | S-10 |
| **状态** | LATER |
| **描述** | 从 MemorySaver 升级到 PostgresSaver，支持断点续跑和多 Worker 实例 |
| **关键文件** | `agent/graph.py` |
| **说明** | 代码已支持（检测 `DATABASE_URL`），待 Postgres 环境配置和验证 |

### S-11：diagkit 独立部署验证

| 字段 | 内容 |
|---|---|
| **编号** | S-11 |
| **状态** | LATER |
| **描述** | 验证 `scripts/` 下的 diagkit 部署脚本在目标环境的一键部署和运维流程 |
| **关键文件** | `scripts/diagkit-*.sh`、`onekey-deploy.sh` |

### K-10：高频问题排查文档持续建设

| 字段 | 内容 |
|---|---|
| **编号** | K-10 |
| **状态** | LATER |
| **描述** | 根据 `docs/06-高频导航问题知识建设任务.md` 的计划，持续推进高频问题排查文档的人版和 Agent 版 |
| **依赖** | 代码仓库补全、报表分析 |
| **说明** | 与知识建设任务协同推进 |

### K-11：onsite-top Agent 文档自动生成

| 字段 | 内容 |
|---|---|
| **编号** | K-11 |
| **状态** | LATER |
| **描述** | 为 `source-docs/onsite-top/` 的人版文档建立 Agent 版自动生成脚本 |
| **依赖** | K-10、代码仓库补全 |
| **说明** | 当前仅有到点精度的手工派生 Agent 文档，无已验证的自动生成器 |

### O-01：LangSmith 可观测性接入

| 字段 | 内容 |
|---|---|
| **编号** | O-01 |
| **状态** | LATER |
| **描述** | 配置 LangSmith 环境变量，零代码接入全链路追踪 |
| **说明** | LangGraph 原生支持，仅需环境变量配置 |

### O-02：诊断闭环统计

| 字段 | 内容 |
|---|---|
| **编号** | O-02 |
| **状态** | LATER |
| **描述** | Case 沉淀统计、同根因检测、Playbook 增强建议 |
| **说明** | 依赖 Case 数据积累到一定规模 |

### T-01：Web UI 端到端测试

| 字段 | 内容 |
|---|---|
| **编号** | T-01 |
| **状态** | LATER |
| **描述** | 在已有静态契约测试基础上补充 Web UI 的端到端或浏览器集成测试 |
| **关键文件** | `tests/test_web_ui.py` |
| **说明** | 当前已有四种模式、Case API、SSE 和报告入口的静态契约测试，尚无真实浏览器交互覆盖 |

### D-20：Demo 基线命名与 Engine 工具边界

| 字段 | 内容 |
|---|---|
| **编号** | D-20 |
| **状态** | LATER |
| **描述** | 保留当前单函数端到端诊断作为 Demo 基线，并拆出采集、信号扫描、错误码分析、路线分析、外部证据与报告渲染的可独立调用边界。 |
| **依赖** | D-01、D-03 |
| **验收** | Demo Case 行为不回归；各函数不依赖 LangGraph State；采集、分析和渲染可被单独单测。 |
| **验证** | `python3 -m unittest discover -s tests -q`；新增 Engine 工作流边界测试。 |

### D-21：路线结果与证据账本契约

| 字段 | 内容 |
|---|---|
| **编号** | D-21 |
| **状态** | LATER |
| **描述** | 定义 `RouteResult` 和 Case 证据账本，要求所有路线结论带证据引用、缺失材料和工具轨迹。 |
| **依赖** | D-20 |
| **验收** | 证据路径不可越出 Case 目录；账本只追加；报告可由证据 ID 回查原始材料。 |
| **验证** | 新增账本写入、去重、无效路径和报告引用单元测试。 |

### D-22：错误码分支 Graph

| 字段 | 内容 |
|---|---|
| **编号** | D-22 |
| **状态** | LATER |
| **描述** | 将单节点图拆为 `collect → signal_scan → error_code_route → evidence_reduce → report`，并为无可信错误码保留明确出口。 |
| **依赖** | D-20、D-21、K-01 |
| **验收** | 一次 Case 只采集一次；有可信错误码时走 Playbook 路线；无错误码不进入错误码路线；节点结果可恢复。 |
| **验证** | 新增 Graph 条件边与 Case 回放测试，覆盖命中、未命中、材料不足。 |

### D-23：无错误码确定性路由器

| 字段 | 内容 |
|---|---|
| **编号** | D-23 |
| **状态** | LATER |
| **描述** | 基于症状、日志文件和信号摘要，在固定路线注册表中选择零到两条路线；首批支持 `navigation` 和 `task_chain`。 |
| **依赖** | D-21、D-22 |
| **验收** | 路由不调用 LLM；输出路线、评分和理由；不允许未注册路线或超过两条路线。 |
| **验证** | 新增路由参数化单测与两个无错误码 Case fixture 回放。 |

### D-24：无错误码路线 Worker 与证据汇合

| 字段 | 内容 |
|---|---|
| **编号** | D-24 |
| **状态** | LATER |
| **描述** | 实现 `navigation`、`task_chain` 两个确定性路线 Worker，以及基于证据账本的汇合和结论状态规则。 |
| **依赖** | D-23 |
| **验收** | 每条路线均返回 `RouteResult`；汇合可识别证据不足和冲突；不能由路线直接覆盖最终结论。 |
| **验证** | 新增路线 Worker、证据冲突、`confirmed/likely/awaiting_human/insufficient_data` 状态测试。 |

### S-12：Harness 执行控制与人工接管

| 字段 | 内容 |
|---|---|
| **编号** | S-12 |
| **状态** | LATER |
| **描述** | 为路线注册、工具白名单、调用预算、节点幂等、审计轨迹和人工交接提供统一执行控制。 |
| **依赖** | D-21、D-22 |
| **验收** | 超时、预算耗尽或缺材料时停止并写入明确交接项；不得记录凭据；重试不重复采集。 |
| **验证** | 新增预算、重启恢复、重复执行和审计脱敏测试。 |

### O-03：路线级 Case 回放与评测

| 字段 | 内容 |
|---|---|
| **编号** | O-03 |
| **状态** | LATER |
| **描述** | 基于匿名化真实 Case 建立路线选择、证据可用性、结论状态和人工介入率的回放评测。 |
| **依赖** | D-22、D-24、S-12 |
| **验收** | 至少包含错误码成功、无错误码成功、材料不足/冲突三类 Case；输出可比较的路线指标。 |
| **验证** | 提供固定 fixture 与一条可重复执行的评测命令。 |

---

## 任务索引

| 编号 | 名称 | 状态 |
|---|---|---|
| D-01 | 端到端单节点图编译通过 | NOW |
| D-02 | 四个采集入口接入 engine collectors | NOW |
| D-03 | engine 全部模块迁移完成并可 import | NOW |
| D-04 | 模式注册表与固定白名单 | NOW |
| S-01 | FastAPI Case API + SSE 进度推送 | NOW |
| S-02 | 独立 Worker 文件队列 | NOW |
| S-03 | 双源身份认证 | NOW |
| S-04 | 租户隔离存储 | NOW |
| K-01 | Playbook 加载器 | NOW |
| K-02 | TopK 现场文档关键词匹配 | NOW |
| D-10 | 接入 Playbook 与 TopK 到引擎分析管道 | LATER |
| D-11 | 错误码关联图 | LATER |
| D-12 | 增加 analysis_agent 节点 | LATER |
| D-13 | 增加 fusion_agent 节点 | LATER |
| D-14 | 报告节点接入 engine reporting 模块 | LATER |
| S-10 | Checkpointer 升级 PostgresSaver | LATER |
| S-11 | diagkit 独立部署验证 | LATER |
| K-10 | 高频问题排查文档持续建设 | LATER |
| K-11 | onsite-top Agent 文档自动生成 | LATER |
| O-01 | LangSmith 可观测性接入 | LATER |
| O-02 | 诊断闭环统计 | LATER |
| T-01 | Web UI 端到端测试 | LATER |
| D-20 | Demo 基线命名与 Engine 工具边界 | LATER |
| D-21 | 路线结果与证据账本契约 | LATER |
| D-22 | 错误码分支 Graph | LATER |
| D-23 | 无错误码确定性路由器 | LATER |
| D-24 | 无错误码路线 Worker 与证据汇合 | LATER |
| S-12 | Harness 执行控制与人工接管 | LATER |
| O-03 | 路线级 Case 回放与评测 | LATER |
